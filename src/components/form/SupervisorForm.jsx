import React from "react";
import styled from "styled-components";

const SupervisorForm = () => {
  return <StyledDiv className="supervisor-form"></StyledDiv>;
};

const StyledDiv = styled.div`
  &.supervisor-form {
  }
`;

export default SupervisorForm;
